{% extends 'base.html.twig' %}

{% block title %}
	{{circuit.title}}
{% endblock %}
{% block css %}
	<link href="{{ asset('assets/css/circuit.css') }}" rel="stylesheet"/>
	<link href="{{ asset('assets/css/styles.css') }}" rel="stylesheet"/>
	<link href="{{ asset('assets/css/navbar.css') }}" rel="stylesheet"/>
{% endblock %}
{% block content %}
	<div class="container mt-4 mb-4">
		<h1 class="text-center" style=" padding: 2rem 1rem;margin-bottom: 2rem;background-color: #e9ecef;border-radius: .3rem;opacity:0.8;color:#7f5539;font-weight:bold">{{circuit.title}}</h1>
	
			<div class="row mt-2 mb-4" style="padding: 2rem 1rem;margin-bottom: 2rem;background-color: #e9ecef;border-radius: .3rem;">


				<div class="col-4 mt-5">
					<img src="{{ vich_uploader_asset(circuit,'imageFile') }}" class="card-img-top mt-5" alt="..." width="100%" height="40%"/></a>
				{# <img src="{{circuit.picture}}" class="card-img-top" alt="..."> #}


				{# <img src="{{ vich_uploader_asset(circuit,'imageFile') }}" class="card-img-top mt-4" alt="..." width="100%" height="40%"/></a>   #}
				<img src="{{circuit.picture}}" class="card-img-top mt-3" alt="..." width="100%" height="40%">

				</div>
				<div class="col md-6 jumbotron " style="height:100vh">
				<h1 class="text-center text-warning" style="font-size: 3rem;font-weight:bold;">{{circuit.title}}</h1>
			
				<p class="mt-4">{{circuit.description |nl2br}}</p>
				        <div class="text-warning text-center mt-5" style="font-size: 4rem;font-weight:bold;height:100vh; ">
						
			<p class="" style="color:red">	{{circuit.price}}€</p>

			<div class=" text-center d-flex justify-content-between">
		<a href="{{ path('cart_add', {id:circuit.id}) }}" class="btn btn-sm btn-success">Ajouter au panier</a>
		<a href="{{ path('circuit')}}" class="btn mt-2 btn-md btn-secondary">Retour aux circuits</a>
		</div>
	  
					  </div>
				</div>

			</div>
		</div>
	</div>
{% endblock %}
